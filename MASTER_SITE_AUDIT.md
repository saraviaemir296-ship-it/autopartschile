# MASTER_SITE_AUDIT.md — AutopartsChile.cl

Auditoría integral iniciada 2026-08-12. Este documento se actualiza por ciclos, sin sobrescribir historial. Metodología: cada fila tiene evidencia verificable (archivo/sha/comando), no opinión. Donde no hay evidencia directa (requiere navegador real, Lighthouse, o acceso a GA4/GSC/Merchant Center en vivo) se marca `PENDIENTE DE VERIFICACIÓN` explícitamente — no se inventa un resultado.

## Nota metodológica honesta

El pedido original de esta auditoría (40 secciones: seguridad, pagos, UX, CRO, mobile, performance, accesibilidad WCAG 2.2 AA, analytics, legal, arquitectura, escalabilidad a 20 marcas) es del tamaño de un engagement de consultoría de varias semanas con un equipo completo. Este ciclo cubre lo que es verificable de forma segura y con evidencia real desde el repositorio (seguridad de configuración, pagos, SEO/datos estructurados, arquitectura de datos) porque son auditables por inspección de código sin riesgo. Lo que requiere interacción visual real con el sitio en un navegador (UX, CRO, mobile, accesibilidad, performance/Core Web Vitals) queda marcado como pendiente para el próximo ciclo, donde se puede usar el navegador conectado para capturar evidencia real en vez de inferir desde el código.

## Ciclo 1 — 2026-08-12 (Seguridad + Pagos + Datos, base de código)

| Área | Problema / Hallazgo | Severidad | Impacto | Evidencia | Solución | Estado |
|---|---|---|---|---|---|---|
| Seguridad — Headers HTTP | Headers de seguridad completos ya activos: X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy, Permissions-Policy, HSTS (preload), CSP **activo** (no Report-Only) | — | Positivo, ya resuelto en tareas históricas #173/#179 | `netlify.toml`, sha `1695283d4a93657481410b91c754d663160a96d1` | N/A | ✅ Verificado este ciclo |
| Seguridad — CSP | CSP usa `'unsafe-inline'` en `script-src` porque el sitio no tiene build step (scripts inline sin nonce). Esto reduce la protección real contra XSS aunque el CSP esté activo. | P2 | Si algún vector de inyección HTML apareciera (ej. un formulario mal sanitizado), `unsafe-inline` permitiría que un script inyectado se ejecute igual | `netlify.toml` línea CSP, comentario propio ya documenta esta limitación | Migrar a nonces requiere introducir un build step (actualmente el sitio es HTML estático puro) — cambio de arquitectura, no un fix de una línea. Documentar como deuda técnica aceptada. | Documentado, no resuelto (decisión consciente ya tomada en tarea histórica #179) |
| Seguridad — Secretos | No se encontraron API keys, tokens ni contraseñas hardcodeadas en `checkout.js` ni `netlify.toml`. El único identificador expuesto es la URL del proyecto Supabase (`zlyforhywunqhitrdreo.supabase.co`), que es pública por diseño (se usa con RLS + anon key, no es un secreto) | — | — | `checkout.js` sha `7168fd7ae4c3c3266ac1c046c60856dc73303216` | N/A | ✅ Verificado este ciclo (revisión de 2 archivos clave; Edge Functions auditadas en Ciclo 2, ver abajo) |
| Pagos — Mercado Pago | Integrado y llamando a un endpoint real de Supabase Edge Function (`mp-create-preference`), no simulado. Redirige a `init_point` real de Mercado Pago. | — | — | `checkout.js` líneas del flujo `openModal`/`fetch` | N/A | ✅ Verificado este ciclo |
| Pagos — Webpay Plus | Integrado a nivel de código (`webpay-create-transaction`, redirect POST con `token_ws`), pero la tarea histórica #210 registra "Pasar Webpay Plus a producción (pendiente credenciales Transbank)" como **pendiente**. | Ver Ciclo 2 — reclasificado con evidencia de código real | — | Tarea histórica #210 | Ver Ciclo 2 | Ver Ciclo 2 |
| Datos — `products.json` vs HTML real | Confirmado en el ciclo SEO paralelo de esta sesión: 119 productos reales (con precio/SKU/CTA) existen en el HTML de 2 páginas pero no en `products.json` | **P1** | Pérdida de visibilidad real en Google Shopping / listados gratuitos | Ver `SEO_AGENT_LOG.md` Ciclo 3 | Ver Ciclo 2 (reclasificado con `INVENTORY_RECONCILIATION.md`) | Reclasificado, ver Ciclo 2 |
| SEO — Schema faltante | 3 páginas con producto real y precio visible no tenían schema `Product`/`ItemList` (swift-indio, swift-15-2010, sx4-hatchback) | P2 | Producto no elegible para rich results / Shopping orgánico | Ver `SEO_AGENT_LOG.md` Ciclo 3 | Agregado ese ciclo; **precios corregidos en Ciclo 2** (ver abajo, PRICE_MISMATCH) | ✅ Resuelto, con corrección adicional en Ciclo 2 |
| SEO — Schema faltante (mayor escala) | `celerio-k10b-azul.html` tiene 55 productos reales sin ningún schema `Product`/`ItemList` | P1 | 55 productos sin elegibilidad a rich results | `repuestos-suzuki-celerio-k10b-azul.html` | Gateado a que se resuelva primero la arquitectura de datos (ver `INVENTORY_RECONCILIATION.md`) | Pendiente, tarea #293 |
| Arquitectura — orphan files | 4 archivos de prueba en la raíz del repo sin valor | P4 | Cosmético | Listado de raíz del repo | Requiere `delete_file`, no disponible | **BLOQUEADO — limitación de herramienta** |
| Dependencias | El sitio es HTML/CSS/JS estático sin `package.json`. | — | Bajo | Estructura del repo | N/A | ✅ Confirmado (frontend). Edge Functions auditadas en Ciclo 2. |

## Ciclo 2 — 2026-08-12 (Auditoría directa de Supabase: Edge Functions, RLS, inventario real)

Este ciclo lee directamente el código fuente de las 4 Edge Functions de pago y la tabla `products` completa (242 filas) vía herramientas de Supabase conectadas — cierra la brecha explícitamente dejada pendiente en Ciclo 1.

| Área | Problema / Hallazgo | Severidad | Impacto | Evidencia | Solución | Estado |
|---|---|---|---|---|---|---|
| Pagos — Webpay Plus (reclasificado) | El código SÍ distingue integración/producción vía `WEBPAY_ENV` con guarda de seguridad (no usa credenciales de test en modo "production" por accidente). Pero el valor real de esa variable de entorno no es legible desde ninguna herramienta conectada. | **P0** | Si `WEBPAY_ENV` nunca se seteó a producción, el sitio muestra el botón a clientes reales pero cobra contra el ambiente de integración de Transbank | `PAYMENT_SECURITY_AUDIT.md`, código completo de `webpay-create-transaction` | Confirmar en el panel de Supabase (Edge Functions → Secrets) | **BLOQUEADO — requiere confirmación del usuario en el panel de Supabase, no verificable por código.** Reemplaza la entrada equivalente de Ciclo 1 con evidencia de código real en vez de inferencia. |
| Pagos — validación de precio | Confirmado: ninguna de las 4 Edge Functions confía en un precio enviado por el navegador. El precio siempre se recalcula server-side desde `products.price_sale` por SKU. | — | — | `PAYMENT_SECURITY_AUDIT.md` | N/A | ✅ Verificado seguro |
| Pagos — PRICE_MISMATCH | 3 SKU (SW-002, SW-004, SX4-002) tenían precio distinto en HTML vs. Supabase — 2 de ellos (SW-004, SX4-002) cobraban MÁS de lo que el HTML mostraba antes del fix, riesgo de reclamo bajo Ley 19.496 | **P1** | Cliente pagaba un monto distinto al anunciado en pantalla | `INVENTORY_RECONCILIATION.md` sección 3 | Alineado HTML/schema al precio real de Supabase | ✅ Corregido este ciclo (commits `de5cb88`, `96f2584`, `e446cb0`) |
| Pagos — MP webhook fail-open | Si `MP_WEBHOOK_SECRET` no está seteado, la verificación de firma se salta, pero el webhook siempre re-verifica el pago real contra la API de Mercado Pago antes de actuar | P2 | Bajo — mitigado por diseño | `PAYMENT_SECURITY_AUDIT.md` | Setear `MP_WEBHOOK_SECRET` por defensa en profundidad | Documentado, no ejecutado (requiere acceso a Supabase Secrets) |
| Pagos — CORS Webpay | `webpay-create-transaction` no tiene el mismo candado de origen que ya existe en `mp-create-preference` | P2 | Bajo — precio igual se recalcula server-side | `PAYMENT_SECURITY_AUDIT.md` | Alinear `Access-Control-Allow-Origin` | Documentado, no ejecutado |
| Pagos — stock no se descuenta | Ni `webpay-confirm` ni `mp-webhook` actualizan `products.stock`/`is_active` al confirmar un pago. Riesgo real dado que la mayoría de piezas tienen `stock:1` (piezas únicas de desarme) — dos clientes podrían pagar la misma pieza física | **P1** | Venta duplicada de un ítem único, reembolso/conflicto con cliente | `PAYMENT_SECURITY_AUDIT.md` | Requiere decisión de lógica de negocio antes de tocar código de pago en producción | Documentado, no ejecutado — pendiente decisión del usuario |
| Seguridad — RLS deshabilitado | `public.oportunidades` tiene RLS **deshabilitado** (nivel ERROR de Supabase advisors). 0 filas actualmente, pero sin protección si la función `radar-oportunidades` empieza a escribir | **P1** | Datos potencialmente expuestos a roles anon/authenticated sin restricción | `get_advisors(security)` de Supabase | `ALTER TABLE public.oportunidades ENABLE ROW LEVEL SECURITY;` | **Presentado al usuario, no auto-aplicado** (regla explícita de la herramienta de Supabase) |
| Seguridad — extensión mal ubicada | `pg_net` instalado en schema `public` en vez de uno separado | P3 | Bajo, no urgente | `get_advisors(security)` | Mover a otro schema | No ejecutado, no urgente |
| Datos — inventario real desconectado | ≥16 SKUs activos en Supabase (CEL12 x33 + 15 más, incluyendo motores/cajas completas de $300k-$850k) no aparecen en NINGUNA página HTML, ni siquiera como "0 productos" | **P1** | Inventario real de alto valor invisible para clientes | `INVENTORY_RECONCILIATION.md` sección 5 | Confirmar vigencia con Emir antes de publicar | Documentado, no ejecutado — requiere confirmación humana antes de publicar (productos de alto valor) |
| Datos — DZ13 checkout roto | Los 66 productos Dzire 2013 tienen botón "Pagar ahora" en el HTML pero **no existen en Supabase** — el pago falla con error al hacer clic | **P0** | Checkout roto en 66 productos ya publicados y promocionados como comprables online | `INVENTORY_RECONCILIATION.md` sección 4.2 | Insertar en Supabase con validación (Fase 5), no ejecutado sin confirmación | Documentado, no ejecutado — requiere confirmación de vigencia antes de dar de alta |
| Datos — CELAZ sync | 53/55 productos de celerio-k10b-azul existen en Supabase pero no en `products.json`/feed | P1 | Pérdida de visibilidad Shopping, no afecta checkout (SÍ están en Supabase) | `INVENTORY_RECONCILIATION.md` sección 4.1 | Regenerar products.json/feed.xml desde Supabase | Documentado, no ejecutado este ciclo |

## Áreas explícitamente NO auditadas todavía (requieren evidencia real, no inferencia)

- **Performance / Core Web Vitals (LCP, INP, CLS, TTFB)**: requiere prueba real en navegador.
- **UX/UI visual, mobile-first, CRO**: requiere screenshots reales del sitio renderizado.
- **Accesibilidad (WCAG 2.2 AA)**: requiere inspección con herramienta axe/Lighthouse contra el DOM renderizado.
- **Búsqueda interna**: requiere probar consultas reales contra el buscador del sitio en vivo.
- **GA4 / Search Console**: sigue bloqueado por falla de la extensión de Chrome.
- **`feed.xml` completo**: no se pudo releer este ciclo por límite de tamaño de la herramienta de lectura conectada y bloqueo de red a `raw.githubusercontent.com` desde el sandbox.

## Severidad de hallazgos abiertos (resumen actualizado tras Ciclo 2)

- **P0**: estado de producción de Webpay no verificable desde el repositorio (requiere confirmación en Supabase); checkout roto en los 66 productos Dzire 2013 (no existen en Supabase).
- **P1**: RLS deshabilitado en `oportunidades`; stock no se descuenta al confirmar pago (venta duplicada de pieza única); inventario real desconectado (≥16 SKU, incluye motores/cajas de alto valor); sync CELAZ pendiente (53 productos); schema faltante en celerio-k10b-azul (55 productos, gateado a resolver arquitectura de datos primero).
- **P2**: CSP con `unsafe-inline`; CORS sin restringir en Webpay; `MP_WEBHOOK_SECRET` fail-open (mitigado).
- **P3**: `pg_net` en schema público.
- **P4**: 4 archivos de prueba huérfanos (bloqueado por herramienta).

**Corregido este ciclo**: PRICE_MISMATCH en SW-002/SW-004/SX4-002 (ver `INVENTORY_RECONCILIATION.md`).
